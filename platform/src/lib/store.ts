/** Re-export origin/main in-memory store. */
export {
  acknowledgePermit,
  analyzeProject,
  awardScope,
  createDemoProject,
  getProjectDetail,
  getSubWork,
  listProjects,
  postScope,
  resetStore,
} from "@/lib/demo/store";
